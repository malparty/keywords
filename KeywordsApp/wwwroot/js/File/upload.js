$(function () {
  $("#csvFileInput").change(() => {
    alert("Thats what you got to do!");
    // check file extension and type + limit in INPUT  first
    // Send upload
    // Show waiter
    // Implement server side model validation
    // Show error or Success message
  });
});
